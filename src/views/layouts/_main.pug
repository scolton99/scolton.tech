include ../shared/_util

block setup
doctype html
html
    head
        meta(charset='utf-8')
        title= page_title
        +style('application')
    body
        block content
            script(type='module').
                import { v4 as UUID_V4 } from 'https://jspm.dev/uuid';
                window.UUID_V4 = UUID_V4;
            +script('App')